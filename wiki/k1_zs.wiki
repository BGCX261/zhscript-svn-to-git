#summary 代码：类似 killall 通过文字杀死进程

= 类似 killall 通过文字杀死进程的脚本 =
{{{
#!/usr/bin/zhscript --。
加载lib/redireconsoi4。
赋予返回以调用‘重定向输出’、“ps ax”。
如果‘参数数目’等于0那么先
	显示‘返回’下原样
类似 killall 通过文字而不是进程号杀死进程，但文字不仅可以是进程名，可以是命令行的任意一段
也接收多个参数来同时杀死多个，使用方法如
k1 windows #查看要执行的杀死wine的命令
k1 windows|sh #执行
上原样。
	结束。
了。
加载lib/defi。
加载lib/regexpr4。
调用‘正则攫’、‘返回’、[“ ”]*([^↙]+)↙、行。
删除返回。
加载lib/file4。
赋予自己以“/usr/bin/zhscript --。 ”调用‘最终文件名’、‘参数0’。
加载lib/gjk4。
加载lib/gjke4。
赋予行号以0。
循环先
	赋予行号以算术‘行号’+1。
	如果‘行号’大于‘行数目’那么跳出。
	别名行以行之‘行号’。
	赋予号以0。
	循环先
		赋予号以算术‘号’+1。
		如果‘号’大于‘参数数目’那么跳出。
		别名参以参数‘号’。
		调用‘正则攫’、‘行’、([0-9]+).+[0-9]+:[0-9]+“ ”(.*先调用‘加反斜杠’、‘参’、R了.*)、果。
		如果‘果数目’大于0那么先
			如果调用‘头匹配方法’、‘果之2’、‘自己’那么再来。
			显示“kill -9 ‘果之1’ # ‘果之2’”↙。
		了。
	了。
了。

}}}