#summary 代码：在用命令删除文件时再确认下对话框

= 在用命令删除文件时再确认下对话框 =
{{{
#!/usr/lib/zhscript/gtkmmsh --。
加载lib/gjke4。
迭代解释下代码
	别名文件以参数1。
	加载lib/doscmd4。
	循环先
		如果调用‘existdir’、‘文件’那么赋予类型以目录，跳出。
		如果调用‘exist’、‘文件’那么赋予类型以文件，跳出。
		赋予类型以。跳出。
	了。
	加载lib2/msgbox。
	赋予择以隧道提示、“是否删除‘类型’ ‘文件’”、“删除文件确认框”、‘询问’、‘是否按钮’。
	加载lib/defi。
	如果‘择’等于‘X’那么跳出。
	当‘择’先
		等于‘是’那么先
			赋予命令以“rm ”先如果‘类型’等于目录那么“-rf ”了'‘文件’'。
			显示‘命令’↙。
			赋予回以执行‘命令’。
			如果‘回’那么隧道提示、‘命令’↙返回‘回’、“删除文件确认框”、‘警告’。
			跳出。
		了。
		等于‘否’那么先
			显示“#‘文件’”↙。
			跳出。
		了。
	了。
上代码、‘参数栈’。
隧道终止。

}}}