#summary 代码：修改dns

= 代码：修改dns =
linux下dns的修改是即时生效，于是用脚本来做便很具使用中的灵活性

adsl拨号的情况下文件也总是会被自动改写
{{{
#!/usr/bin/zhscript --。
加载lib/doscmd4。
加载lib/defi。
加载lib/gjk4。
加载lib/gjke4。
加载lib/clpars4。

赋予1以8.8.4.4。赋予-1以。
赋予2以8.8.8.8。
赋予3以2001:470:20::2。

赋予文件以/etc/resolv.conf。
赋予增加以1。
赋予去掉以0。
赋予单独以0。
调用‘命令行加回调’、
	+、下代码
		赋予增加【主】以1。（默认）
	上代码、0、、
	-、下代码
		赋予去掉【主】以1。
	上代码、0、、
	+1 | +2 | +3 | -1 | -2 | -3、“对 ‘1’, ‘2’, ‘3’ 单独选择增去”、0、下代码
		如果不‘单独’那么先
			赋予单独【主】以1。
			删除-1【主】。
			赋予增加【主】以赋予去掉【主】以0。
		了。
		赋予‘参数0’【主】以。
	上代码、
	l、下代码
		赋予增加【主】以赋予去掉【主】以0。
	上代码、0、、
	、、0、下代码
		显示“‘参数0【主】’ 可选项”↙调用‘命令行帮助’。
		结束。
	上代码。
调用‘命令行解析’、‘参数栈’。

赋予内容以调用‘type’、‘文件’。
显示“‘文件’ 目前内容为：”↙‘内容’↙。
赋予改以0。
序迭代解释下代码
	别名号以参数1。
	赋予行以“nameserver ‘参数2’”。
	如果‘去掉’或者存在-‘号’那么先
		如果调用‘串位置方法’、‘内容’、‘行’大于等于0那么先
			赋予内容【主】以调用‘串替换方法’、‘内容’、‘行’↙、。
			赋予改【主】以1。
		了。
		再来。
	了。
	如果‘增加’或者存在+‘号’那么先
		如果调用‘串位置方法’、‘内容’、‘行’小于0那么先
			赋予内容【主】以‘行’↙‘内容’。
			赋予改【主】以1。
		了。
	了。
上代码、‘1’、‘2’、‘3’。

如果‘改’那么先
	显示“将改为：”↙‘内容’↙写。
	赋予错以调用‘echo’、‘内容’、‘文件’。
	如果‘错’那么
		显示“失败，错‘错’”↙。
	否则
		显示成功↙。
了。

}}}